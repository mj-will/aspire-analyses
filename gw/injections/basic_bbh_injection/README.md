## Basic test of poppy with bilby_pipe

This analyses is a basic test of poppy's integration within bilby and bilby_pipe.

The initial samples are drawn from the prior rather than using an
existing analysis.